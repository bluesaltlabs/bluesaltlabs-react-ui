import React from 'react'

export function BaseInput(props) {
    const { fieldKey, fieldValue, fieldLabel, value, onChange, ...rest } = props


    return (
        <div>BaseInput component</div>
    )
}